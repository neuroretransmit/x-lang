#include "codegen_tests.h"

#include <string.h>
#include <grammar/ast.h>

#include "../grammar/helper.h"

void codegen_var_decl_test()
{
	/*ASTNode* var_decl = mock_ast_node(AST_TYPE_VARIABLE_DECLARATION,
								 mock_ast_variable_declaration(
									mock_token(TOK_TYPE_U8, NULL, mock_token_pos(1, 1), 2),
										mock_token(TOK_IDENT, mock_token_value(TOK_IDENT, strdup("x")), mock_token_pos(1, 4), 1))); */
	
		
}
